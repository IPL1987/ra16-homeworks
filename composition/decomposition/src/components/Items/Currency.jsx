/**
 * Компонент "Курс Валюты"
 * Выводит элемент списка Курсов Валют
 * Принимает props:
 * - название валюты
 * - текущий курс
 * - разница курсов с предыдущим значением
 * - ссылка на страницу графика курса валюты
 */
 export default function Currency({ currency, cost, diff, link }) {
  return (
    <li>
    </li>
  );
}