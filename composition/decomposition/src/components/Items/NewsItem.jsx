/**
 * Компонент "Новости"
 * Выводит элемент списка Новостей
 * Принимает props:
 * - логотип СМИ
 * - заголовок новости
 * - ссылка на новость
 */
 export default function NewsItem({ logo, subject, link }) {
  return (
    <li>
    </li>
  );
}