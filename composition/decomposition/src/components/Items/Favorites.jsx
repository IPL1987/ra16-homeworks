/**
 * Компонент "Посещаемое"
 * Выводит элемент списка Посещаемых Сервисов
 * Принимает props:
 * - название сервиса
 * - краткое описание
 * - ссылка на сервис
 */
 export default function Favorites({ title, description, link }) {
  return (
    <li>
    </li>
  );
}