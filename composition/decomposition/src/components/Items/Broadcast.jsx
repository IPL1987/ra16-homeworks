/**
 * Компонент "Эфир"
 * Выводит элемент списка Эфиров
 * Принимает props:
 * - телепрограмма
 * - название эфирного канала
 * - ссылка на страницу вещания
 */
 export default function Broadcast({ telecast, channel, link }) {
  return (
    <li>
    </li>
  );
}