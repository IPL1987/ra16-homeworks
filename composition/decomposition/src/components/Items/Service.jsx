/**
 * Компонент "Внутренний Сервис"
 * Выводит элемент списка Внутренних Сервисов
 * Принимает props:
 * - название сервиса
 * - ссылка на сервис
 */
 export default function Service({ title, link }) {
  return (
    <li>
    </li>
  );
}